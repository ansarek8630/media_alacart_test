import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceLight = Color(0xFF2C2C2C);
  static const Color primary = Color(0xFF00E5FF); // Teal/Cyan accent
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFAAAAAA);
  static const appBar = Color(0xFF1C1C1E);

  // Channel Colors for Charts
  static const Color channelSearch = Color(0xFF00E5FF); // Teal
  static const Color channelSocial = Color(0xFF2196F3); // Blue
  static const Color channelDisplay = Color(0xFF9C27B0); // Purple

  // Status Colors
  static const Color statusActive = Colors.green;
  static const Color statusPaused = Colors.orange;
  static const Color statusEnded = Colors.red;
  static const Color statusBackground = Color(0xFF2A3A35);

  // navigation bar colors
  static const navActive = Color(0xFF00E5FF);
  static const navInactive = Color(0xFF636366);
  static const navBorder = Color(0xFF2C2C30);

  static const Color success = Color(0xFF4CAF50); // Green for Active/Positive
  static const Color info = Color(0xFF2196F3); // Blue for Objective pill
  static const Color error = Color(0xFFE53935);
}
