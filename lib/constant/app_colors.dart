import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const background    = Color(0xFF121214);
  static const surface       = Color(0xFF1E1E22);
  static const appBar        = Color(0xFF1C1C1E);
  static const cardBorder    = Color(0xFF2C2C30);
  static const statBg        = Color(0xFF252528);
  static const searchBg      = Color(0xFF2A2A2E);

  static const textPrimary   = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFEBEBF5);
  static const textMuted     = Color(0xFF636366);

  static const accent        = Color(0xFF00C8A0);   // teal — active state & progress
  static const accentPaused  = Color(0xFFFFA000);   // amber — paused state
  static const badgeActive   = Color(0xFF00C864);
  static const badgeActiveBg = Color(0x1A00C864);
  static const badgePaused   = Color(0xFFFFA000);
  static const badgePausedBg = Color(0x1AFFA000);
  static const typeLabel     = Color(0xFF00C8A0);

  static const navActive     = Color(0xFF00C8A0);
  static const navInactive   = Color(0xFF636366);
  static const navBorder     = Color(0xFF2C2C30);
}